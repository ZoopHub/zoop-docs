---
title: Settings
description: "Configure your business: hours, service area, tax rates, payments, and more."
audience: end-user
---

Settings is where you tell Zoop about your business: your name and logo, where you work, when you're open, and how you get paid. You only need to do this once when you start, and you can come back any time to change something.

Go to **Settings** in the left navigation menu. A sidebar on the left lists each section — select a section name to open it.

<Image src="https://doc-assets.zoop.pro/images/using-zoop/settings/settings-sidebar-nav.png" alt="Settings sidebar showing Business info, Service area & hours, Scheduling, Payment methods, Notifications, AI & automation, API & MCP, Card payments, and Tax rates sections" />

## Business info

Set your company name, logo, contact details, address, trade types, tagline, and brand color.

**Company name** — saved independently of the rest of the form. Type a name and select **Save name**.

**Storefront URL** — your public booking page address (`app.zoop.pro/your-slug`). Use lowercase letters, numbers, and hyphens — for example, `app.zoop.pro/joes-plumbing`. It must be 3–64 characters. Select **Save URL** after you type it. You must set a URL before you can publish your storefront.

**Enable public storefront** — toggle this on to make your storefront live. When off, your storefront returns a 404 and is omitted from search engines. Your management pages, invoices, and customer portal links are unaffected.

**Logo** — shown on your storefront, invoices, and customer messages.

**Trade types** — check all that apply (Plumbing, HVAC, Electrical, Landscaping, Cleaning, Roofing, General, Garage door, Pest control, Other).

**Brand color** — used on your customer-facing storefront. Enter a hex value or use the color picker.

**Contact information** — business phone, email, and website.

**Business address** — start typing and pick from the autocomplete suggestions, or fill in the fields manually.

Select **Save changes** to apply.

## Service area & hours

Tell Zoop where you work and when you're open.

**Service radius** — how far you travel from your base location, in miles (1–200).

**Service ZIP codes** — if you add specific ZIP codes, your storefront will only show your business to customers in those areas (instead of using the radius above). Type a ZIP code and press Enter (or select **Add**). Remove any by selecting the × on the tag.

**Business hours** — tap the toggle next to each day to turn it on or off, then set your open and close times. The time picker runs from 6:00 AM to 10:00 PM in 30-minute steps.

**Time zone** — all your open/close times are in this zone. Customers see "open now" based on your local clock, not theirs.

**After-hours message** — shown to customers who request service outside business hours (max 500 characters).

**Emergency dispatch** — when enabled, customers can request after-hours emergency service. You can set a dispatch fee that shows alongside the emergency option.

Select **Save changes** to apply.

## Scheduling

Control how jobs are created and how customers self-book.

**Default job duration** — the time block Zoop uses when you create a new job (15 min – 4 hr).

**Buffer between jobs** — travel and prep time automatically added between scheduled jobs (none, 15 min, 30 min, 45 min, or 60 min).

**Advance booking window** — how far in advance customers can book a job through your storefront (1–365 days). For example, set 30 if you want to accept bookings up to a month out.

**Late reschedule cutoff** — how close to an appointment customers can reschedule on their own, without calling you. Set to "no cutoff" to let them reschedule any time, or up to 48 hours in advance.

**Online booking** — enable the booking widget to show a booking button on your storefront. Zoop holds the selected slot until you confirm. You can also set the slot length shown to customers (30 min – 2 hr) and how long a pending slot is reserved before it expires (4–72 hours).

**Technician calendar permissions** — choose which event types your technicians can add to the calendar: Meeting, Block time, Personal, Other.

Select **Save changes** to apply.

## Payment methods

Choose which payment methods you accept and set up late-payment reminders.

**Accepted payment methods** — check any that apply:

- Stripe (credit/debit card) — lets customers pay by card online. Requires card payment setup; see [Card payments](#card-payments) below
- Cash
- Check
- Zelle
- Venmo
- Cash App
- Other

When you enable Zelle, Venmo, Cash App, or Other, extra fields appear for your handle, cashtag, or a custom payment link (e.g. Square or PayPal).

**Late payment reminders** — automatically send reminders to customers with unpaid invoices. Toggle on 3 days, 7 days, and/or 14 days after due date.

Select **Save changes** to apply.

## Card payments

Connect Stripe to accept credit and debit card payments from customers. Stripe is the payment processor Zoop uses — you'll create a free Stripe account during setup. Funds land in your bank account on Stripe's standard payout schedule (typically 2 business days).

<Steps>
  <Step title="Open card payments">
    In the settings sidebar, select **Card payments**.
  </Step>
  <Step title="Start onboarding">
    Select **Set up payments**. You'll be taken to Stripe's onboarding flow, which takes about 5 minutes.
  </Step>
  <Step title="Finish on Stripe">
    Complete Stripe's identity and business verification steps. This is standard for any payment processor — Stripe needs to confirm who you are. When done, you're returned to Zoop automatically.
  </Step>
  <Step title="Check status">
    Zoop shows a checklist of any remaining requirements. If Stripe still needs something, select **Finish on Stripe** to resume. Select **Refresh status** to pull the latest from Stripe at any time.
  </Step>
</Steps>

Once active, the card payments panel shows a green **Active** badge with your charges and payout status. You can open your Stripe dashboard directly from here.

**Offline payment instructions** — add a note (up to 4,000 characters) for customers who pay by check, Zelle, or another offline method. This text appears on invoices alongside online-payment options.

<Callout type="info">
Disconnecting Stripe from within Zoop isn't available right now — the control is hidden while a safe disconnect-and-reconnect flow is built. See [Getting paid](/using-zoop/payments#disconnect-stripe).
</Callout>

## Tax rates

Set up the sales-tax rates you charge customers. Adding a rate here means Zoop can apply it to invoices automatically — you won't have to calculate tax by hand. One rate can be marked as the default for new invoices.

<Steps>
  <Step title="Open tax rates">
    In the settings sidebar, select **Tax rates**.
  </Step>
  <Step title="Add a rate">
    Select **Add tax rate**. Enter a name (e.g. "TX Sales Tax") and a percentage (e.g. 8.25 for 8.25%). Check **Default for new invoices** if this rate should apply automatically.
  </Step>
  <Step title="Save">
    Select **Save**. The rate appears in the active rates table.
  </Step>
</Steps>

Each rate's row has icon actions: select the **pencil** icon to edit, or the **trash** icon to archive it. Archived rates are hidden from the main table — they're not deleted. To bring one back, expand the **Archived** section at the bottom and select the **restore** icon.

To change the default rate, select the radio button in the **Default for new invoices** column of the rate you want to use.

<Callout type="info">
Editing a rate does not change past invoices. Zoop snapshots the rate name and percentage at the time an invoice is issued.
</Callout>

## Notifications

Control which automatic messages Zoop sends to your customers.

<Callout type="info">
**Coming soon.** Notification preferences aren't live yet — these controls are locked until sending is ready. On the Notifications tab they appear switched off and greyed out, with a **Coming soon** badge, and **Save changes** is disabled.
</Callout>

**SMS notifications** — send job confirmations, reminders, and invoice alerts via text message.

**Email notifications** — send copies of invoices and receipts by email.

**Auto-text customers when a job is likely to run late** — when a job with a set arrival window looks like it will run late, Zoop automatically texts the customer a heads-up. This is off by default; when it's off, you or your techs send that message manually from Route manager.

**Review prompts** — ask customers for a review after a job. (The review-prompt timing and review-link fields stay hidden until notifications go live.)

## AI assistant & automation

The AI receptionist, knowledge base, and maintenance reminders are **coming soon** — the settings tab for them isn't available in your account yet. See [Receptionist & automations](/ai/automations) for what's on the way.

## API & MCP

This is the **API access** panel. It lets you connect ChatGPT, Claude, or your own tools to Zoop so they can work with your jobs, customers, and invoices — with permissions you control.

If you've never connected an outside tool to Zoop, you can skip this section.

<Image src="https://doc-assets.zoop.pro/images/using-zoop/settings/api-mcp-panel-v2.png" alt="The API & MCP settings panel showing the MCP server address with a Copy button, the Connect a client tabs (ChatGPT, Claude, Claude Code), and the API keys and Connected apps cards." />

### Your MCP server address

The panel shows **Your MCP server address** with a **Copy** button (it reads **Copied** for a moment after you tap it). This is the address you paste into ChatGPT, Claude, or another tool so it knows how to reach your Zoop account.

### Connect a client

The **Connect a client** switch has three tabs:

- **ChatGPT** and **Claude** connect over OAuth. Add a connector in that tool, paste your MCP server address, then sign in to Zoop and approve what it can access. Nothing happens to your data until you approve.
- **Claude Code** connects with an API key instead. An owner creates the key from the API keys card below.

### What a connection can do

A connected app or key can only ever do what the person who connected it is allowed to do. If a tech can't see your customer list, no app that tech connects can see it either. To widen or tighten what someone — and anything they connect — can reach, set their access on the [Team page](/using-zoop/team).

### Manage cards (owners only)

Two cards at the bottom are for owners:

- **API keys** — "For Claude Code & scripts. Create with the exact permissions you want; revoke anytime." Select **Manage API keys** to create or revoke keys; see [API keys](/developers/authentication/api-keys) for setup. If you're not an owner, you'll see "Only owners can manage API keys. Ask your owner to create one if you need MCP access."
- **Connected apps** — "See what's connected (ChatGPT, Claude), what it can do, and cut it off." Select **View connected apps** to open the list below. Non-owners see "Only owners can review or revoke connected apps."

### Connected apps

The **Connected apps** view is owner-only. It lists every app your team has connected through OAuth, with the subtext "Apps your team has connected through OAuth. Each app acts with the permissions the member who connected it granted." Before anyone connects anything, it reads "No connected apps yet." and "When a teammate connects an app over OAuth, it shows up here."

Each app shows:

- The app name.
- **Connected by {name} ({role})** — who linked it and their role.
- The permission areas it was granted, as badges like "Customers & contacts: View".
- **Last used {date}**.

For each app, owners can:

- **Manage** — re-adjust what the app can do. You can't grant an app more than the member who connected it is allowed.
- **Revoke** — the trash button. A dialog titled "Revoke {name}?" appears with the body "This app will immediately lose access to your Zoop data. It can be reconnected later by going through OAuth again." Select **Cancel** to back out or **Revoke access** to cut it off.

<Image src="https://doc-assets.zoop.pro/images/using-zoop/settings/connected-apps-v2.png" alt="The external access page showing API keys and the Connected apps list, where each app shows who connected it, its granted permission-area badges, and Manage and Revoke actions." />

Developers who want the full list of permission areas and what each one covers can read the [scopes reference](/developers/authentication/scopes).
