---
title: Recurring billing
description: Charge customers automatically on a schedule with subscription plans.
audience: end-user
---

A plan is a reusable billing template tied to one customer. You define the cadence, the line items, and optionally a card to charge. Zoop generates each invoice automatically and — if auto-pay is set up — charges the card on file without you lifting a finger.

<Callout type="info">TODO(screenshot): Plans list page showing the table with title, customer, cadence, next run, lead time, status, and total per run columns.</Callout>

## How plans work

When you create a plan, Zoop calculates the first invoice date from the cadence you choose. On each scheduled run, it snapshots your current line items into a new invoice, sends it to the customer (the same way as a manual invoice), and — if auto-pay is on — charges the card on file automatically.

The plan keeps a full history of every invoice it has generated. You can see it on the plan detail page.

## Create a plan

<Steps>
  <Step title="Open Plans">
    In the left nav, go to **Plans**, then select **New plan**.
  </Step>
  <Step title="Pick a customer">
    Search for the customer this plan belongs to. A plan's customer cannot be changed after it is saved.
  </Step>
  <Step title="Give it a title">
    Use something descriptive, like "Monthly lawn mow" or "Quarterly HVAC service". This appears on every invoice the plan generates.
  </Step>
  <Step title="Set the cadence">
    Choose how often Zoop should bill. The preset options are:

    - Weekly
    - Bi-weekly
    - Monthly
    - Quarterly
    - Annually

    If none of those fit, switch to **Advanced** and enter a raw RRULE string (for example, `FREQ=WEEKLY;BYDAY=MO,WE,FR`). Most plans don't need this.
  </Step>
  <Step title="Set a lead time (optional)">
    Lead time tells Zoop to generate the invoice a number of days *before* the cadence date. For example, a lead time of 7 means the invoice arrives a week early, giving you time to review it before the period begins. Leave it at 0 to generate on the day itself.
  </Step>
  <Step title="Add line items">
    Add every service or product that should appear on each invoice. You can pull from your pricebook or enter items manually. The total per run shows at the bottom of the form.
  </Step>
  <Step title="Set a default tax rate (optional)">
    If you have tax rates configured in Zoop, pick the one that should apply to this plan's invoices by default. See [Pricebook](/using-zoop/pricebook) for how to set up tax rates.
  </Step>
  <Step title="Add notes (optional)">
    Notes are internal — they appear on the plan and are copied to each generated invoice, but are not shown to the customer.
  </Step>
  <Step title="Save">
    Select **Create plan**. Zoop sets the first invoice date and the plan is active immediately.
  </Step>
</Steps>

<Callout type="info">TODO(screenshot): New plan form showing customer picker, title field, cadence preset selector, lead time field, and line items builder.</Callout>

## Set up auto-pay

Auto-pay lets Zoop charge the customer's card automatically each time an invoice is generated. You need two things before you can turn it on:

1. **A card on file for the customer.** Cards are managed on the customer's profile page. See [Customers](/using-zoop/customers).
2. **Auto-pay consent.** The customer must consent to being charged automatically. Zoop captures this through a formal attestation flow that creates a mandate tied to the plan.

<Callout type="warning">
Auto-pay requires your business name to be set in Zoop settings. The mandate shown to the customer includes your business name; if it isn't set, the "Set up auto-pay" option is disabled. Add a business name in [Settings](/using-zoop/settings) first.
</Callout>

### Capture consent and enable auto-pay

<Steps>
  <Step title="Open the plan">
    Go to **Plans** and open the plan you want to configure.
  </Step>
  <Step title="Set up auto-pay">
    In the **Auto-pay** section, select **Set up auto-pay**. Zoop opens an attestation dialog that presents the mandate terms. Confirm on the customer's behalf. Once confirmed, auto-pay is active and Zoop will charge the card on file on each billing run.
  </Step>
</Steps>

<Callout type="info">
You can also set up auto-pay from the plan editor: open **Edit plan**, pick a payment method under **Payment method**, then select **Set up auto-pay** next to the auto-charge checkbox and check **Auto-charge each invoice** before saving.
</Callout>

<Callout type="info">TODO(screenshot): Auto-pay status card on the plan detail page showing "Auto-pay is on" with card brand and last 4 digits.</Callout>

### What auto-pay requires at run time

Zoop charges the card automatically only when all of the following are true at the moment the invoice is generated:

- The plan's auto-charge flag is on.
- A payment method is selected on the plan.
- An active, non-revoked auto-pay consent exists for that customer and plan.
- The payment method is an active Stripe card with a processor mandate ID captured at consent time.

If any of those conditions aren't met, Zoop still generates and sends the invoice — the customer just pays manually via the invoice link.

## Manage an active plan

Open a plan from the **Plans** list to see its summary, auto-pay status, and invoice history. The **Actions** menu gives you these options:

<Callout type="info">TODO(screenshot): Plan detail page showing the Actions dropdown with Charge now, Skip next, Pause plan, and Cancel plan options.</Callout>

### Charge now

Generates today's invoice immediately and, if auto-pay is on, charges the card on file. The customer gets the invoice email the same way as a scheduled run. Zoop asks you to confirm before charging.

This option is available only when the plan is active.

### Skip next

Skips the upcoming billing period. Zoop won't generate an invoice for that period, and the plan resumes its normal cadence after that. Zoop asks you to confirm because the period cannot be recovered.

This option is available only when the plan is active.

### Pause a plan

Pausing stops Zoop from generating any more invoices until you resume. The plan stays in your list with a **Paused** status. Use this when work is temporarily on hold.

To resume, open the Actions menu and select **Resume plan**. Pausing and resuming are reversible.

### Cancel a plan

Canceling is permanent. Zoop stops generating invoices and the plan moves to **Canceled** status. A canceled plan cannot be resumed. You can always create a new plan for the same customer.

Zoop asks you to confirm before canceling.

## Invoice history

Every invoice generated by a plan appears in the **History** table on the plan detail page. Each row shows:

- The date the invoice was generated
- The invoice number (links to the invoice)
- Status (draft, sent, paid, overdue, void, uncollectible)
- Outcome — whether it was charged via auto-pay, is pending, or failed
- Amount

The history is paginated 20 invoices per page.

## Plan statuses

| Status | What it means |
|---|---|
| Active | Plan is running. Invoices generate on schedule. |
| Paused | Invoice generation is paused. Resume to restart. |
| Canceled | Terminal. No further invoices will be generated. |

## Related pages

- [Customers](/using-zoop/customers) — manage customer profiles and saved cards
- [Invoices](/using-zoop/invoices) — view and manage generated invoices
- [Pricebook](/using-zoop/pricebook) — set up services, products, and tax rates
- [Payments](/using-zoop/payments) — take payment on an invoice
- [Settings](/using-zoop/settings) — add your business name (required for auto-pay)
